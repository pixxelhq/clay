# Checklist 

There are few things which  must be passing before handling the model to marketplace team

1. Build is running locally.
    Run `make docker-image`.

2. Tests are passing locally.
    Run `python3 {Modelname}/test_model.py`

3. Build is passing on github ci.

4. Tests are passing on github ci. [Upcoming]
