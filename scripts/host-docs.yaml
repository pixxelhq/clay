apiVersion: apps/v1
kind: Deployment
metadata:
  name: clay-docs
  namespace: mlops
spec:
  replicas: 1
  selector:
    matchLabels:
      name: clay-docs
  template:
    metadata:
      labels:
        name: clay-docs
    spec:
      containers:
        - name: clay-docs
          image: crk2hub.azurecr.io/clay-documentation:latest
          imagePullPolicy: Always
          resources:
            limits:
              cpu: "500m"
              memory: "500Mi"
            requests:
              cpu: "500m"
              memory: "200Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: clay-docs
  namespace: mlops
  annotations:
    service.beta.kubernetes.io/azure-load-balancer-internal: "true"
spec:
  type: LoadBalancer
  selector:
    name: clay-docs
  ports:
    - port: 80
      targetPort: 8080
---
