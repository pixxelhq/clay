deployment:
  storage:
    - provider: azure
      type: str
      value: some-container
model:
  init:
    - name: a
      type: str
      value: "hello world!"
    - name: x
      type: array
      value:
        - x
        - y
        - z
  inputs:
    - name: i
      type: str

inputs:
  - name: i
    type: str
    default: xyz
    value: "a"
    format: string

outputs:
  - name: res
    format: string
    type: str
